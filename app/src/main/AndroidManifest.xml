<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.demo.DemoApp"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_descriptor"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Demo.Default">
        <activity android:name="com.demo.ui.main.MainActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Adding Navigation Graphs here so that any possible deep links can be observed. -->
            <nav-graph android:value="@navigation/main_nav_graph" />
        </activity>

        <activity
            android:name="com.demo.ui.preferences.PreferenceActivity"
            android:exported="false" />
    </application>

</manifest>